createCanvas(600, 600);
background("white");
}

function draw() {

sfunction setup() {
troke("blue");
fill("red");

// console.log(mouseIsPressed)

if (mouseIsPressed) {
  rect(mouseX, mouseY, 20, 35);
}
}